# 奶茶小铺 — 设计开发文档

> 文档版本：v1.0
> 最后更新：2026-06-25
> 项目类型：微信小程序（原生 + 微信云开发）

---

## 目录

1. [项目概述](#一项目概述)
2. [项目结构](#二项目结构)
3. [页面导航与流程](#三页面导航与流程)
4. [云数据库集合设计](#四云数据库集合设计)
5. [云函数接口](#五云函数接口)
6. [全局数据与状态管理](#六全局数据与状态管理)
7. [页面功能详解](#七页面功能详解)
8. [购物车同步机制](#八购物车同步机制)
9. [样式与视觉](#九样式与视觉)
10. [部署与运维](#十部署与运维)

---

## 一、项目概述

**奶茶小铺** 是一个微信小程序奶茶饮品点单系统，支持商品浏览、规格定制、购物车管理、优惠券、签到积分、在线支付和订单跟踪等完整消费链路。

| 项目 | 说明 |
|------|------|
| 开发框架 | 微信小程序原生 |
| 数据层 | 微信云开发（CloudBase） |
| 云数据库 | 5 个业务集合 |
| 云函数 | 1 个统一业务云函数 |
| 基础库版本 | 2.20.1+ |
| 云环境 ID | `cloudbase-d7g83ke4n84c0550a` |

---

## 二、项目结构

<center>
（插入图片：项目目录树结构截图）
</center>

```
miniprogram/
├── app.js                       # 全局入口：云初始化、全局数据、购物车方法
├── app.json                     # 页面注册、tabBar 配置
├── app.wxss                     # 全局样式
├── envList.js                   # 云环境列表
├── sitemap.json
│
├── utils/
│   └── db.js                    # 云数据库客户端封装（统一调用云函数）
│
├── constants/
│   └── index.js                 # 业务常量：22 款商品、8 个分类、6 种加料、4 张优惠券
│
├── pages/
│   ├── index/                   # 饮品首页 — 分类筛选 + 搜索 + 悬浮购物车
│   ├── detail/                  # 商品详情 — 规格选择 + 加料 + 加购/立即购买
│   ├── cart/                    # 购物车 — 勾选 + 数量 + 删除 + 结算
│   ├── order/                   # 订单确认 — 商品清单 + 优惠券 + 支付方式
│   ├── payment/                 # 支付页 — 金额确认 + 确认/取消支付
│   ├── order-status/            # 订单状态 — 排队倒计时 + 进度展示
│   ├── promotion/               # 活动页 — Banner + 新品/爆款推荐 + 领券
│   └── usercenter/              # 我的 — 用户信息 + 签到 + 优惠券 + 订单列表
│
└── cloudfunctions/
    └── cloud-business/          # 业务云函数（统一处理 5 个集合的 CRUD）
        ├── index.js
        └── package.json
```

---

## 三、页面导航与流程

### 3.1 页面路由

| 页面 | 路径 | 导航方式 | 说明 |
|------|------|----------|------|
| 🏠 饮品首页 | `pages/index/index` | tabBar | 左右分栏，展示 8 个分类 |
| 📄 商品详情 | `pages/detail/detail` | navigateTo | 规格 / 加料 / 数量选择 |
| 🛒 购物车 | `pages/cart/cart` | navigateTo | 悬浮按钮 / 列表内跳转 |
| 🎉 活动页 | `pages/promotion/promotion` | tabBar | Banner + 商品推荐 + 领券 |
| 👤 我的 | `pages/usercenter/usercenter` | tabBar | 签到 / 优惠券 / 订单 |
| 📝 订单确认 | `pages/order/order` | navigateTo | 选商品 / 选优惠券 / 提交 |
| 💳 支付 | `pages/payment/payment` | redirectTo | 确认 / 取消支付 |
| ⏳ 订单状态 | `pages/order-status/order-status` | navigateTo | 倒计时 / 进度条 |

### 3.2 核心业务流程

<center>
（插入图片：核心业务流程图 — 首页 → 商品 → 购物车 → 订单 → 支付 → 状态）
</center>

```
首页浏览 → 商品详情 → 加入购物车
                                    ↘
                                      ＞ 购物车（勾选）→ 订单确认
                                    ↗                           ↓
首页悬浮购物车 ———————————————————                     选择优惠券 → 提交订单
                                                               ↓
                                                         支付页面
                                                        ↙        ↘
                                                  确认支付      取消支付
                                                     ↓            ↓
                                               订单状态页      返回上一页
                                               （倒计时制作中）
```

### 3.3 TabBar 结构

| Tab | 页面 | 图标 | 选中图标 |
|-----|------|------|----------|
| 饮品 | `pages/index/index` | `home.png` | `home-active.png` |
| 活动 | `pages/promotion/promotion` | `business.png` | `business-active.png` |
| 我的 | `pages/usercenter/usercenter` | `usercenter.png` | `usercenter-active.png` |

<center>
（插入图片：TabBar 效果截图）
</center>

---

## 四、云数据库集合设计

全部数据通过云函数 `cloud-business` 读写，以用户微信 `openid` 作为 `userId` 关联。

### 4.1 `cart` — 购物车

**操作方式：** 先删除用户全部记录，再批量写入（全量覆盖）。

<center>
（插入图片：购物车页面截图）
</center>

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | ✓ | 用户 openid |
| `key` | string | ✓ | 去重键 `${id}_${size}_${sweetness}_${temperature}` |
| `id` | string | ✓ | 饮品 ID（关联 constants） |
| `name` | string | ✓ | 饮品名称 |
| `image` | string | ✓ | 饮品 Emoji 图标 |
| `bgGradient` | string | ✓ | 卡片背景渐变色 CSS |
| `sweetness` | string | | 糖度（微糖/半糖/三分糖/无糖） |
| `temperature` | string | | 温度（热/温/冰/去冰） |
| `size` | string | ✓ | 杯型（中杯/大杯/超大杯） |
| `sizeExtraPrice` | number | ✓ | 杯型加价（0/3/5） |
| `toppings` | string[] | | 加料名称列表 |
| `toppingTotal` | number | | 加料总价 |
| `quantity` | number | ✓ | 数量（1-99） |
| `unitPrice` | number | ✓ | 单价（不含加料） |
| `totalPrice` | number | ✓ | 该行总价 |
| `checked` | bool | ✓ | 购物车勾选状态 |
| `createdAt` | date | | 创建时间 |
| `updatedAt` | date | | 更新时间 |

### 4.2 `user_coupons` — 用户优惠券

<center>
（插入图片：优惠券列表截图）
</center>

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | ✓ | 用户 openid |
| `couponId` | string | ✓ | 优惠券 ID（c1-c4） |
| `name` | string | ✓ | 满减名称（如"满20减5"） |
| `threshold` | number | ✓ | 满减门槛金额 |
| `reduce` | number | ✓ | 减免金额 |
| `type` | string | ✓ | 类型（满减/新客） |
| `status` | string | ✓ | `unused` / `used` |
| `receivedAt` | date | ✓ | 领取时间 |
| `usedAt` | date | | 使用时间（可为空） |

**优惠券定义（constants/index.js）：**

| ID | 名称 | 门槛 | 减免 | 类型 |
|----|------|------|------|------|
| c1 | 满20减5 | ¥20 | ¥5 | 满减 |
| c2 | 满30减8 | ¥30 | ¥8 | 满减 |
| c3 | 满50减15 | ¥50 | ¥15 | 满减 |
| c4 | 首单立减5 | ¥10 | ¥5 | 新客 |

### 4.3 `user_signin` — 签到记录

<center>
（插入图片：签到页面截图）
</center>

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | ✓ | 用户 openid |
| `streak` | number | ✓ | 连续签到天数 |
| `lastDate` | string | ✓ | 最后签到日期（DateString） |
| `points` | number | ✓ | 累计签到积分 |
| `history` | string[] | | 签到历史日期列表 |
| `createdAt` | date | | 创建时间 |
| `updatedAt` | date | | 更新时间 |

### 4.4 `orders` — 订单

<center>
（插入图片：订单确认页截图 + 订单状态页截图）
</center>

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | ✓ | 用户 openid |
| `orderNo` | string | ✓ | 订单编号（NT + 时间戳 + 4 位随机码） |
| `items` | array | ✓ | 商品快照数组 |
| `totalAmount` | number | ✓ | 原价合计 |
| `finalAmount` | number | ✓ | 优惠后实付金额 |
| `couponUsed` | object | | 使用的优惠券 `{id, _id}` |
| `status` | number | ✓ | 0=待付款, 3=已取消, 4=已付款, 2=制作完成 |
| `paymentMethod` | string | ✓ | 支付方式（默认 wechat） |
| `createTime` | string | ✓ | ISO 格式创建时间 |
| `createdAt` | date | | 数据库创建时间 |
| `updatedAt` | date | | 更新时间 |

**订单状态枚举：**

| 值 | 状态 | 说明 |
|----|------|------|
| 0 | 待付款 | 刚创建，未支付 |
| 4 | 已付款 | 支付成功，制作中 |
| 2 | 制作完成 | 可取货 |
| 3 | 已取消 | 用户取消 |

**items 商品快照结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 饮品 ID |
| `name` | string | 饮品名称 |
| `image` | string | Emoji 图标 |
| `sweetness` | string | 糖度 |
| `temperature` | string | 温度 |
| `size` | string | 杯型 |
| `quantity` | number | 数量 |
| `totalPrice` | number | 小计 |

### 4.5 `users` — 用户信息

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | ✓ | 用户 openid |
| `nickname` | string | ✓ | 昵称（默认"奶茶爱好者"） |
| `avatar` | string | ✓ | 头像 Emoji（默认"😊"） |
| `uid` | string | ✓ | 用户编号 |
| `points` | number | ✓ | 总积分 |
| `createdAt` | date | | 创建时间 |

### 4.6 集合关系图

<center>
（插入图片：5 个集合的 ER 关系图）
</center>

```
users (1) ──────< (N) orders          # 一个用户多个订单
users (1) ──────< (N) user_coupons    # 一个用户多张优惠券
users (1) ──────< (N) user_signin     # 一个用户一条签到记录（全量覆盖）
users (1) ──────< (N) cart            # 一个用户多条购物车商品（全量覆盖）
```

---

## 五、云函数接口

云函数名：`cloud-business` | 入口文件：`cloudfunctions/cloud-business/index.js`

### 5.1 调用方式

客户端通过 `utils/db.js` 封装的 `_call(type, data)` 统一调用，自动解包 `res.result`。

```javascript
// 客户端调用示例
const db = require('../../utils/db');
db.getCart().then(res => {
  // res 直接是 { success: true, data: [...] }
});
```

### 5.2 接口列表

| type | 请求参数 | 返回字段 | 说明 |
|------|----------|----------|------|
| `getCart` | — | `{data: items}` | 获取用户购物车 |
| `updateCart` | `{items}` | `{success}` | 全量覆盖购物车 |
| `getUserCoupons` | — | `{data: coupons}` | 获取用户优惠券列表 |
| `receiveCoupon` | `{coupon}` | `{success}` | 领取优惠券（自动去重） |
| `useCoupon` | `{recordId}` | `{success}` | 标记优惠券为已使用 |
| `getSignInInfo` | — | `{data: {streak, points, lastDate, canSignIn}}` | 查询签到信息 |
| `signIn` | — | `{data: record}` | 签到（+10 积分，连续 +1 天） |
| `getOrders` | `{status?}` | `{data: orders}` | 查询订单列表（可选按状态筛选） |
| `createOrder` | `{order}` | `{orderId}` | 创建订单 |
| `updateOrderStatus` | `{orderId / orderNo, status}` | `{success}` | 更新订单状态 |
| `getUserInfo` | — | `{data: user}` | 获取用户信息 |
| `initUser` | — | `{success}` | 初始化用户信息（首次使用） |

### 5.3 统一返回格式

```json
{
  "success": true,
  "data": {},
  "orderId": "",
  "errMsg": ""
}
```

### 5.4 云函数内部路由

<center>
（插入图片：云函数内部 switch-case 路由逻辑图）
</center>

```
exports.main(event)
  ├── event.type === 'getCart'            → cart.where({userId})
  ├── event.type === 'updateCart'         → cart.remove + cart.add（先删后写）
  ├── event.type === 'getUserCoupons'     → user_coupons.where({userId})
  ├── event.type === 'receiveCoupon'      → 查重 → user_coupons.add
  ├── event.type === 'useCoupon'          → user_coupons.doc.update({status:'used'})
  ├── event.type === 'getSignInInfo'      → user_signin.where({userId})
  ├── event.type === 'signIn'             → user_signin.add/update（+10分，+1天）
  ├── event.type === 'getOrders'          → orders.where({userId})
  ├── event.type === 'createOrder'        → orders.add
  ├── event.type === 'updateOrderStatus'  → orders.doc.update({status})
  ├── event.type === 'getUserInfo'        → users.where({userId})
  └── event.type === 'initUser'           → users.add（不存在时）
```

---

## 六、全局数据与状态管理

### 6.1 app.globalData

<center>
（插入图片：App 生命周期与数据加载时序图）
</center>

| 字段 | 类型 | 默认值 | 加载时机 | 说明 |
|------|------|--------|----------|------|
| `userInfo` | object | `null` | onLaunch | 用户信息 `{nickname, avatar, uid}` |
| `openid` | string | `''` | onLaunch | 微信 openid |
| `cart` | array | `[]` | onLaunch + onShow 刷新 | 购物车本地副本 |
| `points` | number | `0` | onLaunch | 积分 |
| `signInStreak` | number | `0` | onLaunch | 签到连续天数 |
| `canSignIn` | bool | `true` | onLaunch | 今日是否可签到 |
| `tabBarIndex` | number | `0` | — | TabBar 选中索引 |

### 6.2 启动加载流程

<center>
（插入图片：启动加载时序图）
</center>

```
onLaunch
  ├── wx.cloud.init()
  ├── cloud.callFunction('getUserInfo')
  │     └── 获取 openid
  ├── Promise.all([
  │     _loadUserInfo(),    → db.getUserInfo()
  │     _loadCart(),        → db.getCart()
  │     _loadSignIn()       → db.getSignInInfo()
  │   ])
  └── 完成初始化
```

### 6.3 app.js 暴露方法

| 方法 | 参数 | 功能 | 云同步 |
|------|------|------|--------|
| `addToCart(item)` | `{id, name, ..., quantity}` | 添加商品，同规格合并数量 | ✓ 全量覆盖 |
| `updateCart(key, quantity)` | `key, quantity` | 修改数量（≤0 删除） | ✓ 全量覆盖 |
| `clearCart()` | — | 清空购物车 | ✓ 写入空数组 |
| `getCartCount()` | — | 计算购物车总件数 | ✗ 仅本地 |

---

## 七、页面功能详解

### 7.1 首页 `pages/index/index`

<center>
（插入图片：首页完整截图 — 左侧分类 + 右侧商品 + 悬浮购物车）
</center>

**功能列表：**

| 功能 | 说明 |
|------|------|
| 分类侧边栏 | 8 个分类（全部/新品/爆款/咖啡/茶饮/水果/酸奶/甜品）+ 搜索分词 |
| 商品网格 | 每个分类独立分组，支持滚动锚点 `scroll-into-view` |
| 搜索框 | 按饮品名称或描述模糊搜索，实时过滤 |
| 悬浮购物车 | 右下角圆形按钮，显示当前购物车总件数角标，点击跳转购物车页 |

**数据来源：** `constants/index.js` 的 `drinks` 数组（本地静态数据）
**购物车角标：** onShow 时从云端拉取购物车数量双重保障

### 7.2 商品详情 `pages/detail/detail`

<center>
（插入图片：商品详情页完整截图 — 展示规格选择 + 加料 + 底部操作栏）
</center>

| 区域 | 内容 | 交互 |
|------|------|------|
| 商品大图 | Emoji 图标 + 渐变色背景 | — |
| 基本信息 | 名称、描述、月销量、热度标签 | — |
| 主要成分 | 成分标签列表 | — |
| 甜度选择 | 微糖/半糖/三分糖/无糖 | 点击高亮切换 |
| 温度选择 | 热/温/冰/去冰 | 点击高亮切换 |
| 容量选择 | 中杯/大杯/超大杯 | 点击高亮切换，杯型加价联动总价 |
| 加料选择 | 珍珠/椰果/红豆/芋圆/布丁/仙草 | 点击切换选中态，加价联动总价 |
| 数量 | 加减按钮（1-99） | 同步更新总价 |
| 底部操作栏 | 总价 + "加入购物车"按钮 + "立即购买"按钮 | 加购→调用 app.addToCart 云同步；立即购买→跳转订单页 |

### 7.3 购物车 `pages/cart/cart`

<center>
（插入图片：购物车页面截图 — 有商品状态 + 空购物车状态）
</center>

| 功能 | 交互 | 云同步 |
|------|------|--------|
| 商品列表 | 显示所有添加过的饮品（名称/规格/数量/价格） | onShow 从云端加载 |
| 单个勾选 | 点击圆形 checkbox 切换选中态 | ✓ `db.updateCart` |
| 全选/全不选 | 底部全选 checkbox | ✓ `db.updateCart` |
| 数量增减 | 加减按钮（1-99），实时更新小计 | ✓ `db.updateCart` |
| 删除商品 | 点击右侧 × | ✓ `db.updateCart` |
| 去结算 | 仅传递勾选商品到订单页 | — |
| 点击商品图 | 跳转商品详情页 | — |
| 空状态 | 显示"购物车空空如也"提示 | — |

### 7.4 订单确认 `pages/order/order`

<center>
（插入图片：订单确认页截图 — 商品清单 + 优惠券下拉 + 提交按钮）
</center>

| 区域 | 说明 |
|------|------|
| 商品清单 | 显示选中的商品（名称/规格/数量/价格），可继续调整数量 |
| 优惠券 | 下拉列表展示 unused 状态的优惠券，选择后自动计算减免 |
| 价格明细 | 商品合计 → 优惠减免 → 应付金额 |
| 支付方式 | 微信支付（目前仅展示，后续可扩展） |
| 提交按钮 | 校验 → 创建订单（status=0）→ 标记优惠券已使用 → 清空购物车 → 跳转支付页 |

**进入方式：**
- 从购物车进入（`?cart=1`）→ 仅加载 checked 的商品
- 从商品详情"立即购买"进入 → 加载单个商品

### 7.5 支付 `pages/payment/payment`

<center>
（插入图片：支付页截图 — 顶部摘要 + 支付方式 + 底部按钮）
</center>

| 区域 | 说明 |
|------|------|
| 顶部摘要 | 订单号 + 应付金额（大字突出） |
| 支付方式 | 微信支付（高亮选中态） |
| 提示信息 | "确认支付后订单将进入制作流程" |
| 取消按钮 | 白底灰边 → `updateOrderStatus(orderNo, 0)` → 返回上一页 |
| 确认支付按钮 | 橙色背景 → `updateOrderStatus(orderNo, 4)` → 跳转订单状态页 |

### 7.6 订单状态 `pages/order-status/order-status`

<center>
（插入图片：订单状态页截图 — 进度条 + 倒计时）
</center>

| 区域 | 说明 |
|------|------|
| 成功动画 | 圆形勾号动画 + "订单提交成功" |
| 信息卡片 | 订单号 / 前方排队单数 / 预计取单时间 / 制作倒计时 |
| 进度条 | 三步：已下单 → 制作中 → 可取货 |
| 温馨提示 | 取货提醒 + 客服联系提示 |
| 底部按钮 | 返回首页 / 继续逛逛 |

**倒计时逻辑：** 每秒减 1，归零后自动更新订单 status=2（制作完成）。

### 7.7 活动页 `pages/promotion/promotion`

<center>
（插入图片：活动页完整截图 — Banner + 新品推荐 + 人气爆款 + 优惠券）
</center>

| 区域 | 数据来源 | 交互 |
|------|----------|------|
| Banner 轮播 | constants `banners` | 点击跳转首页 |
| 新品推荐 | drinks 中 `category === 'new'`（3 款） | 横向滚动，点击跳详情 |
| 人气爆款 | drinks 中 `category === 'hot'`（4 款） | 横向滚动，点击跳详情 |
| 优惠券列表 | 云端 `user_coupons` + constants `couponList` | 领取/已领取状态切换 |
| 新人专享 | 静态卡片 | Toast 提示 |

**优惠券领取逻辑：**
1. onShow 时从云端加载用户优惠券 → 存入 `receivedIds` 数组
2. 点击领取 → 调用 `db.receiveCoupon()` → 云端去重 → 刷新列表
3. WXML 中通过 `receivedIds.indexOf(item.id)` 判断已领取状态

### 7.8 我的 `pages/usercenter/usercenter`

<center>
（插入图片：我的页面截图 — 头部 + 数据面板 + 优惠券 + 订单）
</center>

| 区域 | 数据来源 | 说明 |
|------|----------|------|
| 用户头部 | 云端 `users` 集合 | 头像 + 昵称 + UID + 签到按钮 |
| 数据面板 | 云端多个集合聚合 | 积分/优惠券数/订单数/收藏数 |
| 我的优惠券 | 云端 `user_coupons` | 列表展示已领取的优惠券 |
| 我的订单 | 云端 `orders` 集合 | Tabs 筛选（全部/待付款/待取货/已完成） |
| 功能入口 | 静态配置 | 收货地址/联系客服/意见反馈/关于我们 |

**签到逻辑：**
1. 点击签到按钮 → 调用 `db.signIn()` → 云端 +10 积分、streak +1
2. 云端判断 `lastDate === today` 决定 `canSignIn`
3. 签到成功后更新页面数据（points / signInStreak / canSignIn）

**订单转换逻辑：**
```
云端 orders 数据 → 本地转换
  items[]         → emojis[] + names[]
  createTime      → 格式化时间字符串
  status (number) → statusText（0→待付款, 4→已付款, 2→已完成, 3→已取消）
```

---

## 八、购物车同步机制

<center>
（插入图片：购物车同步机制数据流图）
</center>

### 8.1 数据流向

```
用户操作（增/删/改/勾选）
        │
        ▼
  app.globalData.cart（内存副本）
        │
        ▼
  db.updateCart(items) → 云函数 → cart 集合（全量覆盖）
        │
        ▼
  onShow → db.getCart() → 重新拉取最新数据
```

### 8.2 同步策略

| 操作 | 本地 | 云端 | 说明 |
|------|------|------|------|
| 添加商品 | 更新 globalData.cart | `updateCart`（全量写） | 即加即同步 |
| 修改数量 | 更新 globalData.cart | `updateCart`（全量写） | 即改即同步 |
| 删除商品 | 更新 globalData.cart | `updateCart`（全量写） | 即删即同步 |
| 勾选/全选 | 更新 data | `updateCart`（全量写） | 即选即同步 |
| 页面加载 | 读 globalData.cart | `getCart`（全量读） |  onShow 拉取 |
| 下单完成 | `clearCart()` | `updateCart([])` | 清空云端 |

### 8.3 降级策略

云端请求失败时，页面使用 `app.globalData.cart` 作为降级数据继续展示，保证基本可用性。

---

## 九、样式与视觉

### 9.1 设计规范

| 项目 | 值 |
|------|------|
| 主题色 | `#FF6B35`（暖橙） |
| 辅色 | `#FFB347`（浅橙） |
| 背景色 | `#F5F5F5` |
| 文字主色 | `#333` |
| 文字辅色 | `#999` / `#ccc` |
| 成功色 | `#52C41A` |
| 危险色 | `#e54545` |
| 圆角 - 卡片 | 16rpx |
| 圆角 - 按钮 | 40rpx+ |
| 圆角 - 标签 | 30rpx |

### 9.2 关键 UI 模式

<center>
（插入图片：UI 组件示例合集 — 按钮/卡片/标签/表单控件）
</center>

- **商品卡片：** 左上 Emoji + 渐变色背景，右下名称/价格/月销量，可选 NEW/HOT 角标
- **规格选择：** 边框圆角按钮，选中态 `active` 类（橙色边框 + 浅橙背景）
- **优惠券卡片：** 虚线边框 + 左右圆形缺口，已领取半透明遮罩
- **底部操作栏：** 固定定位 `position: fixed` + `env(safe-area-inset-bottom)`，阴影分割
- **进度条：** 三步骤横向布局，圆形图标 + 连接线，带动画脉冲效果

### 9.3 动画效果

| 元素 | 动画 | 实现 |
|------|------|------|
| 加购气泡 | 上浮消失 | `@keyframes floatUp` 0.6s |
| 成功勾号 | 弹性缩放 | `@keyframes scaleIn` 0.5s |
| 进度图标 | 脉冲光圈 | `@keyframes pulse` 1.5s infinite |
| 按钮按下 | 透明度降低 | `:active { opacity: 0.85 }` |
| 商品卡片 | 微缩小 | `:active { transform: scale(0.97) }` |

---

## 十、部署与运维

### 10.1 部署步骤

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 打开微信开发者工具 | 导入项目，填入 AppID |
| 2 | 部署云函数 | 右键 `cloudfunctions/cloud-business` → 上传并部署（云端安装依赖） |
| 3 | 创建集合 | 在云控制台创建 5 个集合：`cart`, `user_coupons`, `user_signin`, `orders`, `users` |
| 4 | 验证云环境 ID | `app.js` 中 `wx.cloud.init` 的 `env` 参数需与云环境一致 |
| 5 | 编译预览 | 点击编译，在模拟器中测试完整流程 |

### 10.2 数据维护

| 数据类型 | 存储位置 | 维护方式 |
|----------|----------|----------|
| 商品/分类/规格 | `constants/index.js` | 修改 JS 文件，重新编译 |
| Banner/优惠券定义 | `constants/index.js` | 修改 JS 文件，重新编译 |
| 用户/购物车/签到/订单 | 云数据库 | 云控制台直接管理 |
| 云函数逻辑 | `cloud-business/index.js` | 修改后重新上传部署 |

### 10.3 权限说明

云数据库默认仅创建者可读写。由于当前业务依赖 `openid` 区分用户，建议集合权限设置为：
- **所有用户可读，仅创建者可写** — 可满足当前按用户隔离的数据模型
- 或使用**安全规则**精确控制每条的读写权限（推荐线上环境使用）

---

> **文档结束** — 奶茶小铺 v1.0
